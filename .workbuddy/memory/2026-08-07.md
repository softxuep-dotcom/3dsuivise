## 2026-08-07 难度平衡四项调整

修改文件：
- `src/render/GameRenderer.ts` — `syncDayNight()`
- `src/game/simulation/GameSimulation.ts` — `updateWolves()` / `updateNeeds()`

调整内容（按用户反馈"夜太暗、狼太少、夜间体温下降不明显、饥饿下降慢"）：
1. **夜间亮度**：夜空色 `0x31445f`→`0x4a6080`；半球光夜强度 1.34→1.85；月光强度 0.82→1.45；曝光 1.03→1.12。
2. **狼群数量**：每夜 target `min(72, 42+(d-1)*10)` → `min(90, 40+(d-1)*15)`；刷怪间隔曲线 `0.9~5.7s` → `0.7~4.0s`。
3. **夜间体温流失**：基础速率 1.15/s → 2.4/s → 1.5/s → **体温系统整体重构**（见下）。

## 体温系统重构（2026-08-07 晚）

用户要求重构体温系统，新增"过热死亡"端点和"初始居中"等规则。改动文件：
- `src/game/simulation/GameSimulation.ts`：新增常量、重写 `updateNeeds()`、新增 `endGame()` 和 `deathCause` 字段、改 `update()` 游戏结束判定、改 `getObjective()` 提示。
- `src/ui/HudController.ts`：`showGameOver()` 按死因显示文案；`critical` 样式覆盖两端。
- `index.html`：`warmth-value` 默认 85 → 50。

**新规则**（最终版，2026-08-07 第三次迭代）：
- 初始体温 50（中间值）。
- `WARMTH_MIN=0`（冻死）/ `WARMTH_MAX=100`（过热死亡）。
- **三个独立分量相加**，皮衣和地形**不再**影响体温：
  - 篝火边：+3.0/s（独立，始终为正）
  - 白天：+1.2/s（独立）
  - 夜晚：-1.6/s（独立）
- 组合：白天火边 +4.2/s、白天无火 +1.2/s、夜晚火边 +1.4/s、夜晚无火 -1.6/s。
- 饥饿归零仍扣血（保留），体温端点直接 `endGame()`。
- `deathCause: "frozen" | "overheated" | "killed" | null`。
- HUD 提示：<10"即将冻死"、>90"即将过热"、<25"体温过低"、>75"体温偏高"。

**演进历史**（同一文件内，避免下次重复）：
- v1：火边 +6.2、夜间无火 -1.15×地形×皮衣、白天 +0.32。
- v2：火边 +7.5（>75 时 +3.0）、夜间 -2.4×地形×皮衣、白天 +1.2，皮衣 ×0.7。
- v3（最终）：三个独立分量 +3/+1.2/-1.6，去掉皮衣/地形影响。

**注意**：皮衣现在只影响 `defense+4`，不再缓冲体温；地形（崖穴/风口）完全不影响体温。

4. **饥饿下降**：0.12/s → 0.30/s → **0.55/s**（满→空约 3 分钟）。用户反馈"还是太慢"后再次上调。

验证：`npm run typecheck` 通过；`npm run build` 仅因 sandbox 无法删 `dist/index.html` 失败（与改动无关，dev 模式无影响）。
